#include <bits/stdc++.h>
using namespace std;
int main() {
    long long int t,z,i,n,c,zz=1;
    cin>>t;
    for(z=0;z<t;z++)
    {
        cin>>n;
        printf("Case %d: %lld\n", z+1, -1 + (long long int)(sqrt(n+1)));
    }
    return 0;
}
